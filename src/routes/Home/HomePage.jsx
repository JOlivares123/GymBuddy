import './HomePage.scss'

export const HomePage = ({name='Jacob'}) => {
    return(
        <div>
            <h1 className='text-start pt-5 ps-3'>Welcome {name}</h1>
            <div>

            </div>
        </div>
    )
}