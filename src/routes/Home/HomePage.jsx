import './HomePage.scss'

export const HomePage = ({name='Jacob'}) => {
    return(
        <div>
            <h1 className='bolderText'>Welcome {name}</h1>
            <div>

            </div>
        </div>
    )
}